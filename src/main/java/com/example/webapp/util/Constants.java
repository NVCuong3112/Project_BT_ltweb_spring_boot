package com.example.webapp.util;

/**
 * Lớp chứa các hằng số dùng chung trong hệ thống.
 */
public class Constants {

    // Đường dẫn thư mục lưu trữ ảnh tải lên (bạn có thể tự điền đường dẫn theo máy của bạn)
    public static final String UPLOAD_DIRECTORY = "D:/BT_ltweb/src/main/webapp/uploads";

    // Tên file mặc định nếu không xác định được tên file tải lên
    public static final String DEFAULT_FILENAME = "default.file";
}
